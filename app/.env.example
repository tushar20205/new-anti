# SkillSwap+ frontend environment
#
# Local Vite development and Docker use same-origin /api by default.
# Set this only when the frontend is hosted separately from the backend,
# for example Vercel frontend + Render backend.

# Example:
# VITE_API_URL=https://your-render-service.onrender.com/api
VITE_API_URL=
